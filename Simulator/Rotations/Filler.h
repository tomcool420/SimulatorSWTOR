#pragma once
#include "Simulator/Rotations/Condition.h"
#include "Simulator/libSimulator/types.h"

namespace Simulator {
class Filler {
    using FillterOrder = std::pair<AbilityId, Conditions> void addAbility(const AbilityId &id, Conditions &&conditions);
};
} // namespace Simulator